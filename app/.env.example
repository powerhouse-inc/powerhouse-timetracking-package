# Switchboard GraphQL endpoint (Vetra local default shown)
NEXT_PUBLIC_SWITCHBOARD_URL=http://localhost:4001/graphql

# The drive that holds the TimetrackingWorkspace + Timesheet documents
NEXT_PUBLIC_DRIVE_ID=vetra-690b7ba0

# Renown login URL. Leave empty to use local workspace identities for dev.
NEXT_PUBLIC_RENOWN_URL=
